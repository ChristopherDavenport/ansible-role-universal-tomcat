---
# vars file for universal-tomcat

__tomcat_mirrors:
  - http://apache.claz.org
  - http://mirror.cogentco.com/pub/apache
  - http://mirror.olnevhost.net/pub/apache


# Tomcat major version
__tomcat_version_major: "{{ tomcat_version|truncate(1, True, '') }}"

__tomcat_tar_archive: apache-tomcat-{{ tomcat_version }}.tar.gz
